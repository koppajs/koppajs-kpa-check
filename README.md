# @koppajs/kpa-check

CLI diagnostics runner for KoppaJS `.kpa` files.

## Scope

This package executes the shared diagnostics and workspace-graph layer outside
the editor.

## Development Note

Until `@koppajs/language-core` is published independently, this repo consumes the
adjacent sibling checkout via `file:../language-core`.
CLI diagnostics runner for KoppaJS .kpa files.
